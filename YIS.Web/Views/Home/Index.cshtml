@using YIS.GDPDataHandler.Db;
@model List<IndustryAnnualGDP>

@{
    ViewBag.Title = "Bathos Insights";
}

<h2>Bathos Insights</h2>

<div class="container" id="gdpGraph">
    <button>Click here</button>
</div>

<!--
    <div class="container" id="gdpdata">
    <table class="table table-striped table-bordered" id="gdpdatatable">
       <thead>
           <tr>
               <th>Industry</th>
               <th>GDP Value</th>
               <th>Year</th>
           </tr>
       </thead>
        <tbody>
                @foreach (var entity in Model)
                {
                    <tr>
                        <td>@entity.Sector</td>

                        <td>@entity.Value</td>

                        <td>@entity.DateCode</td>
                    </tr>
                }
        </tbody>
    </table>
</div>
    -->
